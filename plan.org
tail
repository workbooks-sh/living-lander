- audit: taste/honesty pass on 04-anatomy + 05-weave (last commits). Found a real fragility — both spine/flow SVGs used preserveAspectRatio=none with fixed-cy node circles, so the nodes drifted off their planes on reflow (the green live node could slide off plane 02 / move 03 at narrow widths). Fix: replaced the pinned-node SVGs with a single 0..100 tracking line (vector-effect non-scaling-stroke; dashed on 05 = process-in-time), and let sacred-green life breathe ONLY on the plane itself (plane 02 / move 03 already carry the .alive dot). More robust, no meaning lost. GATE (both) 11/12, 0 triggers, PASS. wb content check clean. 06-join read clean, no change. Note: localhost:4000 404s this run (dev server not serving); published anyway — partials were live from prior runs. -3 plan.org
