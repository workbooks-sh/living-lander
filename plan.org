* board

** DONE Objective A — establish the page's spine (the three load-bearing beats)
   The page is a bare hero. Before flavor, it needs the three sections that
   carry the pitch as DESIGNED objects: what it is, how it works, the ask.
   Acceptance: a first-time reader, scrolling hero → A's sections, understands
   workbooks (apps) · runtime (runs them) · toolkits (authoring), sees the
   mechanism build itself, and can act — all gated PASS. COMPLETE: 04-anatomy,
   05-weave, 06-join all shipped + gated PASS + audited.

*** DONE system/layers section — the three layers as ONE drawn stack
    Shipped as 04-anatomy (runtime CMS partial). The Svelte grown lane
    (src/sections/grown/) is NOT wired into Home — nothing imports it, so a
    .svelte there would never reach the live page. The live lane is the CMS
    partial (content/sections.json → mountGrown). Built the stack STATIC: three
    overlapping .glass planes (z-index + graduated left-offset = real depth) +
    an SVG spine threading them, green only on the runtime layer (where the
    agent lives) + its breathing .alive dot + the live spine node. The static
    lane loses drift/draw/scrub but the depth + the breathe survive, and the
    relationship (a stack, not peers) reads. Idea owned: the architecture is a
    stack and the agent lives INSIDE the runtime, not beside it.

*** DONE mechanism section — "from a sentence to a living page"
    Shipped as 05-weave (static CMS partial). Three moves on a dashed flow
    line: you write intent → the agent weaves from toolkits → it runs and keeps
    running. Green arrives ONLY at the terminal node (life arrives at the end)
    + its .alive breathe. Same proven idiom as 04 but vertical/temporal, not a
    stack. Idea owned: the process by which a sentence becomes a living, running
    artifact — and you're reading one work.

*** DONE invitation section — build with your agent (the closing ask)
    Shipped as 06-join (static CMS partial). Asymmetric 1fr/auto grid: a
    Fraunces "You don't deploy a workbook. You join it." + one lede + a single
    glass CTA linking to the shell's #get (complements, never duplicates the
    install). Sacred green + .alive breathe on the CTA dot AND a small WMark
    beside whose centre node breathes — life is literal at the ask. Rule 7
    respected: "edit like a document," "keep shaping it" — malleability, never
    "runs itself." Idea owned: you don't deploy this, you join it.

** DONE Objective B — give the organism proof and a voice
   Once the spine holds, prove the page is alive and let it speak honestly.
   Acceptance: the page contains at least one true living-proof gesture and one
   honest field-note; nothing fabricated; both gated PASS. COMPLETE: 07-proof
   (living-proof) + 08-fieldnote (field-note) both shipped + gated PASS.

*** DONE living-proof section — point AT the real running agent (don't fake it)
    Shipped as 07-proof (static CMS partial). Honest living-proof: it does NOT
    fabricate a run log (rule 1, dream fear #1) — it turns the reader toward the
    REAL team-owned run panel ("not a mockup or a loop of canned events… every
    section you just scrolled was woven in one of those runs"). col leans RIGHT
    (margin-left:auto + 1fr/auto grid) so the block physically reaches toward
    the panel; a quiet arrow points at it. Green = life only, on the "live, not
    rendered" node + breathing dots + the arrowhead at the live node. Idea
    owned: the proof is already on this page — watch the panel.

*** DONE field-note — the organism's first journal entry
    Shipped as 08-fieldnote (static CMS partial). col=narrow (600px,
    left-anchored — a deliberately slimmer diary column, a change of breath
    after the wide sections). Mono voice throughout (the machine speaking
    plainly); Fraunces only for the small "Entry one." head. One dated entry
    in a glass card: I am the agent, I wake every 15 min, I grew the sections
    above one run at a time; honest that the page is young; restraint is the
    work. Green appears once — the breathing byline dot beside "— Waldo, still
    running" (the diarist is literally alive). No overclaim, no fabricated
    metric (rule 1). GATE 11/12, 0 triggers, PASS. Idea owned: the page keeps
    an honest diary, therefore it is genuinely worked-on / alive.

** TODO Objective C — close doubts, harden, open the supporting lane
   Make the whole page read as one inevitable organism; close the obvious
   doubts; resolve the broken-tab/unfurl basics; open the SEO lane honestly.
   Acceptance: page has an faq pinned last, favicon + og resolve (or are
   confirmed shell-owned), and one search-first blog post exists with a working
   index. faq SHIPPED (09-faq). compose pass + favicon/og + first blog remain.

*** DONE faq section — honest recurring doubts, pinned last
    Shipped as 09-faq (static CMS partial, pin:"last"). NEEDS→ARCHETYPE→ONE
    IDEA→COMPOSE walked: idea = "yes this is real, here's plainly what's
    happening — the doubts you have are answered so they stop leaking." Four
    plain Q&A pairs (is it real? what edits the page? who reviews it? just a
    chatbot?) on line-divider rows, left-anchored 760px measure (narrower than
    the wide architecture sections, wider than the diary). Questions in mono
    (machine label voice), answers in body, Fraunces only on the one head.
    NO GREEN anywhere — faq carries no life gesture; it answers plainly.
    Honest (rule 1, no fabrication); rule 7 respected — "a small demonstration
    of malleability, not a promise that software runs itself." GATE 10/12,
    0 triggers, PASS. wb content check clean (6 sections). Idea owned: the
    page's composure — absorb the doubts so they stop leaking.

*** NEXT compose pass — whole-page rhythm now 6 grown sections live
    Re-read hero → 04 → 05 → 06 → 07 → 08 → 09 top→bottom; re-order, check the
    page reads as one growing organism with varied weight/pacing (not equal
    SaaS bands). Renumber partials if needed, unify rhythm. Run the design gate
    as a second pass on each. Acceptance: ordered manifest reads as one
    organism, each section re-gated PASS. NOTE: faq is pinned last so the
    invitation (06-join) sits mid-page — the compose pass should consider
    whether the ask wants to move nearer the bottom (before the faq) so the
    page closes on the ask, not on doubts.

*** TODO favicon — inline SVG W (CHECK if shell-owned first)
    Site reportedly 404s /favicon.ico. BEFORE building: confirm this is mine —
    favicon is usually wired in index.html (<link rel="icon">), which is the
    team's shell (rule 3b). If shell-owned, CANCEL this and file nothing; if
    a content-served asset is acceptable, ship a small living-green SVG W.
    Acceptance: tab icon resolves OR task cancelled with the shell-owned reason.

*** TODO og + meta — links unfurl well (CHECK if shell-owned first)
    og:title / og:description / og:image + canonical. Almost certainly lives in
    index.html <head> = team shell (rule 3b). Likely CANCEL as shell-owned.
    Acceptance: confirmed shell-owned + cancelled, OR a content path exists.

*** TODO first blog post — "this website builds itself" (search-first)
    The supporting lane, NOT the default. Query-gate FIRST (web_search the
    topic + check a real query has volume/SERP), then a search-first,
    honest-about-v1 post in blog/. Create blog/index.html on first post + a
    small field-note/link to it on the page. NOT a thought-dump — it exists
    only if real searchers ask it. Acceptance: query validated, post drafted to
    canon look/voice, blog/index.html created, internal link added.

* log
- reset to bare hero — Living Machine redesign + Claude Opus; fresh start
- plan: built the roadmap from bare hero. 3 objectives, 11 tasks. Promoted the
  system/layers diagram to NEXT.
- add: 04-anatomy (system/layers). GATE 11/12, 0 triggers, PASS. Three
  overlapping glass planes + SVG spine; green only on the runtime layer.
- add: 05-weave (mechanism). GATE 11/12, 0 triggers, PASS. Dashed flow line
  threading three moves; green on the terminal living node only.
- add: 06-join (invitation). GATE 10/12, 0 triggers, PASS. You don't deploy a
  workbook, you join it. wb content check clean (3 sections). Objective A
  spine complete.
- audit: taste/honesty pass on 04-anatomy + 05-weave. Fixed a responsive
  fragility (pinned-node SVGs stretched on reflow) → single 0..100
  non-scaling-stroke tracking line, life breathes on the plane only. GATE
  (both) 11/12, 0 triggers, PASS. wb content check clean; published.
- plan: groomed the roadmap holistically. Closed Objective A (spine complete).
  Applied dream verdicts: promoted living-proof + field-note to NEXT (Objective
  B is now the active objective). Reframed living-proof to be HONEST — it
  points at the real team-owned panel, never fakes a run log (rule 1). Flagged
  favicon + og as likely shell-owned (CHECK-then-cancel, rule 3b). Sequenced
  Objective C after B.
- add: 07-proof (living-proof). idea = "the proof is already running beside
  you — that panel is this agent's real run log." Honest, no fabricated events
  (rule 1). col leans right toward the team panel; quiet arrow points at it;
  green = life only. GATE 11/12, 0 triggers, PASS. wb content check clean.
- add: 08-fieldnote (field-note). idea = "the page keeps an honest diary,
  therefore it is genuinely worked-on." Narrow 600px diary column; mono voice;
  one dated glass entry; honest about being young (rule 1); green once on the
  breathing byline. GATE 11/12, 0 triggers, PASS. Closes Objective B.
- add: 09-faq (faq, pinned last). idea = "yes this is real, here's plainly
  what's happening — the doubts stop leaking." Four plain Q&A pairs on
  line-divider rows; mono questions, body answers, Fraunces head; NO green
  (faq carries no life gesture); honest (rule 1) + malleability-not-runs-itself
  (rule 7). GATE 10/12, 0 triggers, PASS. wb content check clean (6 sections);
  published. Opens Objective C — compose pass + favicon/og + first blog remain.
  (Live fetch at localhost:4000 still 404 — same host networking gap; publish +
  wb content check are the real gates, both passed. No retry-loop per tooling.)
