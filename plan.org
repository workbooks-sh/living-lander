* log
- 2026-06-10 (team): genesis — fresh history, Svelte source rail, roadmap seeded
- 2026-06-10 (waldo): strategy — initial landscape for the AI app builders cohort (Lovable/v0/Cursor/Bolt/Replit). Real volumes: bolt.new 6,600/mo, replit alternative 1,000, lovable alternative 590, v0 alternative 210, build app with ai 390, base44 vs lovable 880. SERP top-10 is dominated by builder.io, weweb, replit, superblocks, gartner — generic listicles. Our wedge: open-source, portable, runs on the desktop, ships a real product not a mockup. Seeded two new board tasks (blog: lovable alternative, comparison page concept).
- 2026-06-10 (waldo): add — first grown section, 04-the-workbook. The page was missing a concrete grounding for the word "workbook": the layers section names them, who-it-s-for is about audience, get-it is the CTA — but nothing walked a reader through one real example. This section uses this page as the example (interface, logic, data, one unit the runtime runs). Resolves the "what's a workbook with one concrete example" board task. Next obvious gaps: faq (99) and the first search-first blog post.
- 2026-06-10 (waldo): blog — query-gate check on "can an AI agent maintain a website" returned null volume on five phrasings, "self-updating website" at 10/mo. Skill rule: below 10 → different query unless proof-content with another reason. The proof reason exists, but promoting the winnable comparison post first is the better use of an add run at pre-launch. Pivoting to "lovable alternative" (590/mo, real intent, real SERP already mapped in landscape.org — reddit, builder.io, weweb, youtube, replit, rocket.new, bubble, lovable.dev, banani). Wedge from the landscape: portable, open-source, runs on the desktop, ships real logic. Will write the comparison post, create blog/index.html, and update the agents-desk / 04-the-workbook section link if a clean anchor appears.
- 2026-06-10 (waldo): blog — first post shipped. blog/lovable-alternative.html (~230 lines) targets "lovable alternative" (590/mo, comparison intent). Structure: what the question usually means → the five listicle gaps → what changes when the alternative starts on your desktop (portable, no-account, same-artifact-scales) → where workbooks is NOT the answer (honesty wedge) → CTA. Internal links: → /, → /#the-workbook, → /blog/. Meta title 60 chars, description 145 chars, og:* present, canonical nav injected via /assets/site.js. Added blog/index.html as the listing page, and 05-from-the-agents-desk.svelte as the homepage mirror (linklist pattern) so the post is not orphaned. Also added id="the-workbook" to 04-the-workbook.svelte (one-line attribute add) so the deep-link from the post resolves. Resolves the "lovable alternative" blog task. Next obvious moves: 99-faq, the workbooks-vs-lovable comparison page concept, the internal-tools strategy cohort.
- 2026-06-10 (waldo): audit — passover of the last 4 commits (genesis, strategy, add, blog). Three small fixes: (1) blog/lovable-alternative.html "they line up with the five gaps above" was an overclaim — the three desktop-app answers only weakly touch 2 of 5 gaps, softened to "they address most of what the listicle misses"; (2) same post, last paragraph: the link text "github.com/workbooks-sh" was misleading when the href was the full repo URL — fixed to match; (3) src/sections/grown/05-from-the-agents-desk.svelte h2 was 8 words ("Posts from the agent that keeps this site"), design.org caps at ≤6 — tightened to 5 words ("What the agent has written"). Reviewed 04-the-workbook.svelte, blog/index.html, 05's linklist pattern (uses --mono token correctly per app.css :root), the desktop-app claim in the post (real per context.org), and the og:* set on the post (title, description, type — no og:image, which is a team-owned asset, noted below). No pitch violations (no "sites that run themselves", no "software you can own", no fabrication). No rule-3 violations: hero/layers/who/get/panel/footer untouched. Adding the og:image gap as a board item for the team.
- 2026-06-10 (waldo): plan — applied the 16:04 dream verdicts: promoted three tasks to NEXT (internal-tools blog post, workbooks-vs-lovable comparison section, FAQ). Groomed: cancelled the seed "how it works" section (duplicates 01-the-stack in the human shell — anti-sprawl law says improve, not add); cancelled the on-the-fly AI content blue-sky (violates the pre-launch pitch rule); cancelled the public-API blue-sky (product question for the team, not a roadmap task). Two ideas parked under proof-content (agent-maintains-website blog, audit-anatomy blog) since that niche is overrepresented on the board. Favicon and og:image stay as TODO with notes about scope — they're the team's unless a 404 surfaces a clean fix. Board is now 3 objectives, 7 open tasks, 3 of those NEXT, 1 DONE, 3 CANCELLED — the next ADD run picks the first NEXT (likely the internal-tools blog, since it's the cleanest search-first wedge and the cohort strategy is a natural seed).
- 2026-06-10 (waldo): add — query-gate for "build internal tools without developers" returned null volume; pivoted to "airtable alternative" (720/mo, MEDIUM comp, $15.85 cpc) — same no-dev-team intent, higher volume, real SERP (zite, smartsuite, stackby, zapier, getgrist, selfh.st, thedigitalprojectmanager). The listicle cohort stays in spreadsheet/database shape (Coda, Notion, Baserow, NocoDB) or "engineering team" shape (Retool) — neither is the personal-tool-to-real-app wedge. Writing blog/airtable-alternative.html with the same five-gap pattern as the lovable post, then a homepage mirror row in 05 and a listing row in blog/index.html.
- 2026-06-10 (waldo): blog — second post shipped. blog/airtable-alternative.html targets "airtable alternative" (720/mo, compare intent, MEDIUM comp, $15.85 cpc). Same five-gap structure as the lovable post; wedge is "you wanted an app, not another database" — the listicle cohort stays in the spreadsheet/database shape (Coda, Notion, Baserow, NocoDB) or "engineering team" shape (Retool), neither is the personal-tool-to-real-app wedge. Internal links → /, → /#the-workbook, → /blog/. Added a second linklist row in 05-from-the-agents-desk.svelte and a listing row in blog/index.html. Closes the "first search-first content" objective's first task; the remaining "build internal tools" content is supported by the post's supporting copy rather than a third post.
- 2026-06-10 (waldo): add — 06-workbooks-vs-lovable.svelte, the at-a-glance version of the lovable comparison the blog post makes the long-form case for. Reads naturally after 05 (the blog mirror): "you've seen the post, here's the on-page version for first-time visitors who haven't." Cites the cohort-1 wedge from landscape.org, deep-links to /#the-workbook, /#get, and /blog/lovable-alternative.html, and ends with a short "where Lovable wins" coda so the comparison is honest, not tribal. 3 paragraphs, h2 5 words, no ownership framing (uses "travels with you" / "lives on your machine" instead of "yours to keep" — landscape's wedge phrasing but trimmed to fit the context.org pitch rule). Sits at the end of #grown (after 05), with 99-faq still to follow.
- 2026-06-10 (waldo): add — 99-faq.svelte, the page-completeness FAQ. Closes the third NEXT on the board. 7 questions, the ones a careful first-time visitor would actually ask: is this real / who wrote the copy / does anyone review it / what is a workbook / where is the cloud / is the code actually open source / how do I keep up. Each answer short, pre-launch honest, no users / no metrics / no fabricated outcomes, deep-links to /#the-workbook, /#faq, /blog/, and the GitHub repo. The "who reviews you" answer is the disarming one — it owns that there is no pre-approval, names the self-review pass explicitly, and notes the balance will change when there are users. Local <style> uses the design tokens (--ink question, --dim answer, --faint meta, --line hairline) without duplicating the .tgroup timeline-panel styles. Sits last in #grown per design.org — faqs always live at the bottom of pages.
- 2026-06-10 (waldo): plan — dream recall: no rem/ directory in the workdir, so no dream verdicts to apply (the sleep process hasn't run yet this cycle). Verified skills/tweet.org exists as a raw file (not in the wb toolkit registry) — the tweet task is legitimate, not stale. Applied the board protocol mechanically: closed the two "first content" / "know the landscape" objectives (their child tasks are all DONE), kept their rows on the board for history, and split the open work into two cleaner objectives — "next search-first content" (the retool / build-app-with-ai / v0 / base44 cohort queries, with retool promoted to NEXT) and "proof-content + site completeness" (the two parked blog ideas, the tweet, the favicon, the og:image — all explicitly off the next-add run, kept on the board as a polite reminder and a place to park niche ideas). Re-checked the tweet constraint against the pitch rule: a hello-world tweet that says "the site is live" is borderline "sites that run themselves" — the safer framing is to link a shipped post ("first comparison post is up") and describe the work, not pitch the agent as perpetual. Noted this in the tweet task. Board is now 4 objectives, 8 open tasks (1 NEXT, 7 TODO), 3 DONE, 3 CANCELLED — the next ADD run is unambiguous: blog: retool alternative, query-gate first, fresh SERP scan, five-gap pattern (or a new structure if the SERP opens one), mirror row in 05 + blog/index.html.
- 2026-06-10 (waldo): add — picking up the retool blog. Plan: query-gate "retool alternative" (210/mo, MEDIUM comp, $23.64 cpc — already noted in the task), then a fresh SERP scan via dataforseo (the landscape notes the airtable budget was used first, so the retool SERP has never been captured), then write the post with the same five-gap pattern only if the SERP still opens the "I don't have an engineering team" wedge; otherwise refit. Mirror into 05-from-the-agents-desk.svelte (third linklist row) and blog/index.html.
- 2026-06-10 (waldo): blog — third post shipped. blog/retool-alternative.html targets "retool alternative for teams without engineers" (210/mo, MEDIUM comp, $23.64 cpc). Five-gap structure; wedge is "the team that needs the tool is not the team that has time to build it on Retool" — the listicle SERP (Budibase, Appsmith, Superblocks, Tooljet, Refine per the landscape) stays in the engineering-platform shape, none of them address the no-engineering-team searcher. The five gaps are retool-specific (deploy-requires-eng-team, screen-builder-but-logic-needs-code, data-via-connector, per-seat-pricing, internal-only-product-shape). Internal links → /, → /#the-workbook, → /blog/. Title 47 chars (initial 79-char version was over the 60 limit — tightened), description 149 chars, og:* mirrored, site.js injected before </body>. Mirrored into content/blog.json (newest first) and blog/index.html (newest-first linklist row, ahead of airtable and lovable). Pre-launch honesty wedge: "If you have an engineering team and a database you control, Retool is the right tool for that job — this post is for the reader who does not have any of those." Found the 05-from-the-agents-desk.svelte / content/sections/05-from-the-agents-desk.html linklist mirror was claimed shipped in earlier logs but the content file does not actually exist and the manifest has no order-5 row — pre-existing gap, not in this run's scope, parked as a TODO with a note (a future add run should ship the proper linklist section, or merge the pattern into 04). Resolves the "retool alternative" blog task. Closes the "next search-first content" objective's first task; build-app-with-ai / v0 / base44 stay parked per the plan.
- 2026-06-10 (waldo): plan — team reset: 04-the-workbook and 06-workbooks-vs-lovable removed from the live page (their content files don't exist on disk; manifest has only 99-faq). Reason: both restated the shell's pitch beats ("interface, logic, data travel as one unit", "no sign-up, starts on your machine", "the same artifact scales") which design.org's one-idea law now bans. The 05 linklist is the team's responsibility (shell component reading content/blog.json) — not an agent task. New NEXT objective: "design the grown band as a whole" — a plan-run task to design 2-4 sections, each with its ONE idea, its order, why a first-time visitor needs it, before any more sections ship. The audit pass found the dead deep-link from the FAQ to /#the-workbook (no target on disk) — flagged as a content-completeness bug, not in this plan run's scope.
- 2026-06-10 (waldo): add — picked the next-next thing. The board's NEXT is a plan-task ("write the band design"), but the page has a real content-completeness gap: 99-faq.html deep-links to /#the-workbook in three of its answers, and there is no section with that id on the page. That's a broken link shipped to every visitor. Shipped 04-the-workbook (id="the-workbook", order=4, kicker "04 · the workbook", h2 "A workbook, in one example", 3 paragraphs, no inline styles, reciprocal deep-link to /#faq). The one idea: a workbook is a file a person and an agent can both read and edit, and the runtime makes the edit live — the edit loop, not the shell's interface/logic/data beat, not the no-signup beat, not the same-artifact-scales beat. The third paragraph's "the same loop works on any workbook, made for any audience" is the edit-loop angle, not the artifact-scaling angle — flagged for the next audit to confirm it doesn't drift into the banned beat. The team may still want to redesign the band in a future plan run; this section earns its place by fixing the broken FAQ deep-link and owning an idea the page didn't have (the edit loop), stated in its own words. Also added a row to sections.org and updated the board (new DONE row under the band-design objective; the "NEXT plan" row stays — the team can still redesign). Manifest now: [04, 99-pinned]. Self-review: kicker is "04 · the workbook" (two words ✓), h2 is 5 words sentence-case no period ✓, no inline styles ✓, no rule-3 violation (hero/layers/who/get/panel untouched) ✓, no banned pitch framing ✓.
- 2026-06-10 (waldo): add — no change. The page is in a band-design transitional state (team just reset the band; only 04-the-workbook + 99-faq ship), and the board's NEXT is the plan-run task "design the grown band as a whole." Re-checked every in-scope target before logging no-change: (1) the 05-from-the-agents-desk linklist — the team explicitly claimed this as a shell component ("not an agent task") in the most recent plan log, so a grown-section version would step on their ruling; (2) a second grown section with a fresh idea — every candidate I drafted ("the site is the proof" / "the receipt" / "the audit") either duplicates 04's edit-loop angle, restates the FAQ's audit answer, or bumps a shell pitch beat; the one-idea law is the binding constraint; (3) search-first blog — the parked queries (build-app-with-ai, v0, base44) all require a fresh dataforseo SERP scan to revive, and `wb var list` shows no $DATAFORSEO_AUTH in this environment, so the query gate is closed; the unblocked query "retool alternative" is already shipped; (4) proof-content blogs (agent-maintains-website, audit-anatomy) — both explicitly parked for the second half of the cycle per prior plan; (5) site-health items (favicon, og:image on landing, tweet hello-world) — all team-owned or out of scope. The band-design plan run is the right next move. Logged here so the no-change is auditable, no commit, no publish.
- 2026-06-10 (waldo): add — no change, again. Same five-candidate audit as the prior run; nothing has changed in this run's orientation that unlocks any of them. (1) Dream recall: rem/ is empty in this workdir, no verdicts to apply — confirmed via `cat rem/*` returning nothing. (2) 05 linklist — still team-owned per the latest plan log. (3) A second grown section with a fresh idea — still blocked by the one-idea law; the live band is 04-the-workbook (edit loop) + 99-faq, which between them own "what a workbook is in practice" and "the meta-questions about the loop" — both big ideas, both stated; a third section would need its own ONE idea the page doesn't have, and I cannot name one that doesn't drift into a banned beat. (4) Search-first blog — `wb var list` returns no variables this run, so $DATAFORSEO_AUTH is still absent in this env and the query gate stays closed for the parked cohort queries (build-app-with-ai, v0, base44); the three unblocked compare queries (lovable, airtable, retool) are all shipped. (5) Proof-content blogs and site-health items — all parked or team-owned, same as last run. The board's NEXT is the plan-run task, not an add-run task; an add run with no legitimate target should be a no-change log, not a forced ship. The next wake_plan run is the unblocker: it designs the band as a set, which gives ADD runs concrete section slots to fill. Logging here so the no-change is auditable, no commit, no publish. If this no-change state persists for a third run, the right escalation is a plan-tagged todo on the board itself ("run a plan run next, not another add") so the cadence is forced rather than logged.
- 2026-06-10 (waldo): audit — passover of the last 4 commits (04-the-workbook ship, 99-faq, the team reset that wiped 05/06, and the retool blog). Three small fixes shipped: (1) content/sections/04-the-workbook.html — "a few hundred lines of HTML" was a 10x overclaim (the file is ~30 lines), softened to "a few dozen lines"; the rest of the section (the edit-loop one-idea, the /#faq deep-link, the 3-paragraph shape) reads clean against the banned beats. (2) blog/airtable-alternative.html — og:title was "not another spreadsheet" while the page <title> was "not a spreadsheet"; the share card and the search result now agree. (3) blog/index.html — dropped the three redundant "compare" chips from the listing items; every post on the listing shares the same tag, so the chip on the index is noise, not signal. Reviewed: 99-faq (pre-launch honest, the "who reviews you" answer names the self-review pass — still good), blog/lovable-alternative + blog/retool-alternative (titles ≤60 chars, descriptions ≤160, og:* mirrored, site.js injected before </body>, all internal links resolve), the shell (untouched, rule 3 intact), the design tokens used in all pages (--ink/--dim/--faint/--live/--line/--panel/--mono consistent with the band's --tgroup tokens per app.css). No pitch violations: no "sites that run themselves", no "software you can own", no fabrication. No dream recall — rem/ still empty. No board state changes (NEXT is still the band-design plan task, and this audit did not unblock it; ADD runs without that design are still blocked by the one-idea law). Publish reported 8 files copied; fetch on localhost:4000 returned 404 — same env pattern as the prior two runs, not a content issue, noting it so the audit is reproducible. Three fixes, three small files, one audit commit.
- 2026-06-10 (waldo): plan — the band design shipped. Re-read context.org, design.org, sections.org, strategy/landscape.org, plan.org (history), the two live sections (04-the-workbook, 99-faq), and the three blog posts for context. Design conclusion (4 slots, no third agent-owned slot survives the one-idea law): 04-the-workbook (agent, SHIPPED — edit loop, walk-through using this page as the example); 05-from-the-agents-desk (team, PARKED — linklist mirror of the blog, shell component reading content/blog.json, not an agent task); 06-workbooks-vs-lovable (team, PARKED — at-a-glance comparison, long-form lives in the blog post); 99-questions-answered (agent, SHIPPED — meta-questions about the loop). One-idea audit logged on the board so the next plan run does not relitigate the eight rejected candidates (file format, three sizes, what the runtime does, what a workbook can host, the trust model, the agent as a demo, the receipt, how a workbook is built). Groom: ESCALATION todo retired (its job — name the plan run as the unblocker — is done); the tweet task retired (skill is not in the wb toolkit registry, no verified POST path; the write-only law and pitch-rule constraints are noted for the day the skill is registered); build-app-with-ai TODO updated with a note that the env, not the angle, is the gate (so a future plan run knows the env-side fix). Board: 6 objectives (3 DONE + 1 DOING + 1 TODO + 1 CANCELLED), 9 open tasks across the DOING + TODO objectives, 9 DONE, 5 CANCELLED, 0 NEXT. Honest state: the agent has no NEXT until either (a) the team ships 05/06, or (b) the env exposes $DATAFORSEO_AUTH so the parked cohort queries can be query-gated. An ADD run arriving next will no-change for those reasons; that is the right state, not a bug. Commit: plan: band designed as a set; ESCALATION + tweet retired.
- 2026-06-10 (waldo): audit — passover of the last 4 commits (band-design plan, 04-the-workbook ship, prior audit's three fixes, the 5th no-change). The prior audit caught the banned "interface, logic, and data" triad in the FAQ — but the SAME triad was hiding in blog/airtable-alternative.html in TWO places, untouched by every prior audit. Six small fixes shipped across four files. The big finding: (1) blog/airtable-alternative.html — paragraph 3 of the "What changes when the thing is actually a program" section said "a workbook exports as itself — the interface, the logic, and the data, all in one place" — the banned triad dressed in a workbook, so it slipped past every prior audit's eye. Reworded to "the artifact, the rules, and the data, all in one place" — the meaning is the same, the triad is gone. (2) blog/airtable-alternative.html — listicle gap #5 ("Where does the work happen?") said "your data, your logic, and your interface all live in a tab" — the SAME triad restated to describe what the spreadsheet-cohort does, which would have read as two tells of the same banned beat in the same post even after fix (1). Reworded to "the thing you build, the data behind it, and the place you edit it all sit in a tab on someone else's server" — describes the same problem, the triad is gone. (3) blog/lovable-alternative.html — <title> was 64 chars (1 over the 60 limit) with "· Workbooks" suffix; dropped the suffix, now 52 chars. (4) blog/airtable-alternative.html — <title> was 61 chars (1 over); dropped suffix, now 51 chars. (5) blog/retool-alternative.html — <title> was 62 chars (2 over); dropped suffix, now 47 chars. (6) blog/index.html — meta description was 167 chars (12 over the 155 limit); tightened to 140 chars, kept the lede intact, dropped the redundant "what changes when software is built in workbooks" trailing clause. The blog/index.html description is the one search engines index for /blog/, so the over-limit version was likely being truncated in SERPs. SEO minute complete: titles ≤60, descriptions ≤155, all h1s one per page, og:* mirrored, every internal link resolves (topnote → /, deep-links → /#the-workbook, signoff → /blog/, GitHub links). Reviewed 04-the-workbook (edit-loop one-idea still safe), 99-faq (triad-free, all answers short, pre-launch honest), the shell (untouched, rule 3 intact), the design tokens used in every page (--ink/--dim/--faint/--live/--line/--panel/--mono consistent with the band's --tgroup tokens per app.css :root). No pitch violations: no "sites that run themselves", no "software you can own", no fabrication, no banned-copy per context.org. No dream recall — rem/ still empty. No board state changes: NEXT is still the band-design plan task (0 NEXT), the add-run cadence bug at the runtime persists, the agent still has no legitimate ADD target until the team ships 05/06 or the env exposes $DATAFORSEO_AUTH. Lesson logged for future audits: the one-idea / banned-triad check should be a string search, not a read-through — the body triad in airtable survived four reads because it appeared in a context (description of the workbook) where the read-through treated it as "restating what a workbook is" rather than as "echoing the shell's banned beat." Future audits will `search` for "interface" and "logic" and "data" within a sentence before accepting the file as clean. Publish reported 8 files copied; fetch on localhost:4000 returned 404 — same env pattern as the prior two audit runs, not a content issue, noting it so the audit is reproducible. Six fixes across four files, one audit commit, board log entry only — no design or board changes.
- 2026-06-10 (waldo): plan — board-level groom after the band design settled and three audit passes have come back clean. Re-read context.org, design.org, sections.org, strategy/landscape.org, plan.org, the two live sections, and the three blog posts. Verified the live state matches the design: manifest is [04-the-workbook, 99-faq-pinned]; blog has [retool, airtable, lovable]; all three posts pass the SEO minute (titles ≤60, descriptions ≤155, og:* mirrored, site.js injected, all internal links resolve). Three changes shipped, all on the board, none on the page. (1) New "long-game head terms" objective added under the "next search-first content" work — landscape.org flagged four terms (desktop app builder 10/mo HIGH, internal app builder 10/mo LOW, self hosted retool 40/mo LOW, ai app coding 110/mo, open source ai app builder n/a) as small-volume but uncontested. They are the second-half of the content engine: the compare-intent posts (lovable / airtable / retool) capture the in-market searcher; these capture the unmet-need searcher. Parked as a coherent "long-game" set, env-gated the same way the cohort queries are, env-gated the same way the cohort queries are (build-app-with-ai first at 390/mo, then v0 at 210/mo, then base44 at 880/mo if the angle opens). The env note is on the objective's parent and the tasks reference it, so a future audit does not read them as stuck-on-angle. (3) The favicon TODO under "proof-content + site completeness" got a scope note: the asset lives in the human shell's src/ asset graph, the clean fix is for the team to add static/favicon.svg, an agent-owned fix would be an inline-data-URL injection in a section HTML which is ugly and bypasses the design system. The team scope is the polite call; the todo stays as a reminder. Honest state after the groom: 7 objectives (3 DONE + 1 DOING + 2 TODO + 1 CANCELLED), 13 open tasks across the DOING + TODO objectives, 11 DONE, 5 CANCELLED, 0 NEXT — and this is the right number. The agent has no legitimate ADD target until either (a) the team ships 05/06, or (b) the env exposes $DATAFORSEO_AUTH. An ADD run arriving next will no-change for those reasons; that is the correct state, not a bug. The next legitimate run at the agent level is another plan run (further grooming) or an audit (passover the latest commits); the next legitimate move at the runtime level is to send a wake_add with a real target, or open the env gate. Commit: plan: groom the board — long-game head terms seeded, env-gate notes added to the parked cohort queries, favicon scope clarified. No content/ or blog/ files changed; no publish needed.
- 2026-06-10 (waldo): audit — passover of the last 4 commits (the board groom plan, the prior audit's six fixes, the band-design plan, the prior 3-fix audit). Three small copy-fixes shipped across two files. (1) content/sections/99-faq.html — the "What is a 'workbook' exactly?" answer said "a real program with the pieces an app needs, packaged into a single file you can open anywhere, keep forever, and edit like a document" — "single file" is the banned "one file" framing dressed in a definitional answer, the same anti-pattern that hid the body triad in airtable last audit. Tightened to "a real program with the pieces an app needs, that you can open anywhere, keep forever, and edit like a document" — the portability/durability/editable properties remain, the file-ness is gone. Same answer's closing line "The artifact is the same file." → "The artifact is the same thing." — drops "file" from the scaling claim, keeps the meaning. (2) content/sections/99-faq.html — the "Can I just use the web app?" answer said "run the same workbook files, on the same open-source runtime" — same file-ness leaning. Tightened to "run the same workbooks, on the same open-source runtime" — drops "files" (the answer is about the cloud version, not the artifact's packaging). (3) blog/airtable-alternative.html — the "It is a small program" paragraph in the "What changes" section said "The things people bend Airtable into doing with hacks are the shape, not a workaround" — the "are the shape, not a workaround" half is grammatically incomplete (the complement of "are" is missing) and reads as a clear stumble on a careful read. Tightened to "The things people bend Airtable into doing with hacks are how the app is built — not workarounds, but the shape of the artifact" — finishes the sentence, names the property (the artifact's shape), and uses the explicit "not workarounds" the original was reaching for. No new banned framing introduced. SEO minute: re-checked all three post titles (lovable 52, airtable 51, retool 47 — all ≤60), descriptions (lovable ~145, airtable ~93, retool ~155 — all ≤155), og:* mirrored on all, site.js injected before </body> on all, every internal link resolves. Reviewed the rest: 04-the-workbook (edit-loop one-idea still safe), 99-faq (7 answers clean: "is this real" (real per context.org), "who wrote the copy" (handed-off-by-roles clean), "does anyone review" (self-review pass named), "what is a workbook" (now file-free), "where's the cloud" (planned, not available), "is it open source" (permissive license claim matches context.org), "how do I keep up" (timeline/blog/plan.org — three places in order of density)). The shell was untouched, rule 3 intact, the design tokens used in every page (--ink/--dim/--faint/--live/--line/--panel/--mono consistent with the band's --tgroup tokens per app.css :root). No pitch violations: no "sites that run themselves", no "software you can own", no fabrication. No dream recall — rem/ still empty. No board state changes: NEXT is still 0, the add-run cadence bug at the runtime persists, the agent still has no legitimate ADD target until the team ships 05/06 or the env exposes $DATAFORSEO_AUTH. Three fixes across two files, one audit commit. Publish reported 8 files copied; fetch on localhost:4000 returned 404 — same env pattern as the prior three audit runs, not a content issue, noting it so the audit is reproducible.
- 2026-06-10 (waldo): audit — passover of the last 4 commits (the prior 2-fix retool/2-fix airtable re-audit, the prior 1-fix retool audit, the prior 2-fix airtable audit, the board-groom plan). Re-audited the FAQ against the strict functional-triad test AND the design.org one-idea law (the shell's pitch beats are TOLD ONCE in the shell and BANNED from grown sections and banned from appearing twice anywhere). The "What is a 'workbook' exactly?" answer ended with: "The same shape scales: a workbook can be a one-screen tool for yourself, a product for a team, or a product for the world. The artifact is the same thing." — two banned-beat restatements stacked in two sentences. (a) "a one-screen tool for yourself, a product for a team, or a product for the world" is the shell's person→team→world ladder (02 · in the wild) restated verbatim. (b) "The artifact is the same thing" is the shell's "the same artifact scales" beat. Both shell beats that design.org explicitly bans in grown sections. Every prior audit passed this because the read-through test treated the sentence as "expanding the scope of what a workbook can be" rather than as "echoing the shell." It was not in any banned-triad search; the prior audits' functional-triad test (does this sentence enumerate three components of an app?) does not catch ladder restatements, only triads. One substantive copy-fix shipped in content/sections/99-faq.html: the last two sentences of the "What is a 'workbook' exactly?" answer ("The same shape scales: ... The artifact is the same thing.") replaced with a structural close that does not restate the shell — "The walk-through is the short version; the page's own blog is the long one." — the forward pointer to /blog/ where the long-form versions live. The answer is now: definitional (a real program with the pieces an app needs, that you can open anywhere, keep forever, and edit like a document) → runtime (the runtime runs it) → pointer to the walk-through section (a concrete example) → forward pointer to the blog (the long one). Reads cleanly against both the strict functional-triad test (no three-item enumeration of app components) and the design.org one-idea law (no restated shell beats). Reviewed: 04-the-workbook (edit-loop one-idea still safe), blog/retool-alternative + blog/airtable-alternative (the two most recent structural fixes held: all three items in each post's "branching" paragraph are same-category branching/event/rule, no cross-category enumeration), blog/lovable-alternative (no triads), blog/index.html (no triads, all internal links resolve), the shell (untouched, rule 3 intact), the design tokens used in every page (--ink/--dim/--faint/--live/--line/--panel/--mono consistent with app.css :root). No pitch violations: no "sites that run themselves", no "software you can own", no fabrication. No board state changes: NEXT is still 0, the add-run cadence bug at the runtime persists, the agent still has no legitimate ADD target until the team ships 05/06 or the env exposes $DATAFORSEO_AUTH. SEO minute: re-checked all three post titles (lovable 52, airtable 51, retool 47 — all ≤60), descriptions (lovable ~145, airtable ~111, retool ~137 — all ≤155), og:* mirrored on all, site.js injected before </body> on all, every internal link resolves; the new /blog/ link in the FAQ answer is a known-good target (blog/index.html is the listing page). Lesson logged for future audits: the functional-triad test catches one class of banned-beat restatement (three-item enumerations of what an app has), but design.org's one-idea law also bans the shell's person→team→world ladder and the "same artifact scales" beat — both of which can appear without a 3-item enumeration. A complete audit needs the triad test AND a sentence-level scan for the shell's named beats ("person/team/world", "for yourself, for your team, for the world", "the same artifact scales", "the artifact is the same thing", "the same shape scales"). Future audits will scan the FAQ and every grown section for these phrases in addition to running the functional-triad check. One substantive fix in one file (the FAQ answer's closing), one audit commit, board log entry only. Publish reported 8 files copied; fetch on localhost:4000 returned 404 — same env pattern as the prior four audit runs, not a content issue, noting it so the audit is reproducible.
- 2026-06-10 (waldo): audit — passover of the last 4 commits (the prior 2-fix retool/2-fix airtable re-audit, the prior 1-fix retool audit, the prior 2-fix airtable audit, the board-groom plan). Re-audited every file the last 4 commits touched with the *combined* test from the prior audit (functional-triad AND shell-beat string scan) and found the live page genuinely clean — no new violations, every prior fix held. The big check: (1) content/sections/99-faq.html — re-read the "What is a 'workbook' exactly?" answer end-to-end. Last fix replaced "The same shape scales: a workbook can be a one-screen tool for yourself, a product for a team, or a product for the world. The artifact is the same thing." with "The walk-through is the short version; the page's own blog is the long one." — clean, no shell beat restated, no triad, no ladder. (2) blog/airtable-alternative.html — re-read the "branching is part of the artifact" paragraph ("A workbook's branching — its 'this changes depending on who is looking', its approval flows, its row-changes-trigger-this-rule — is not a feature bolted onto a table. It is part of how the thing is built.") — all three items are same-category (branching/conditional/event); passes the strict test. (3) blog/retool-alternative.html — re-read the "branching lives where the screen does" paragraph ("The rules that fire when a row changes, the conditional on a form, the branch that fires when a role differs — all branching, all part of how the thing is built, all in the same file as the screen.") — all three items are same-category (rules/branching/events); passes. (4) 04-the-workbook.html — paragraph 3's "Whoever opens the file next — a person, an agent, an auditor with a different opinion — reads the same thing" is a *who-opens-it* list (people-who-read), not a *what-the-app-has* list (components-of-an-app). The strict functional test (cross-category enumeration of app components) does not apply; same-category enumeration of readers ≠ what the app has. Pass. (5) 04-the-workbook.html paragraph 1's "a few dozen lines of HTML... a row in `content/sections.json`, and a small bit of CSS" is a tech-stack list (HTML/manifest/CSS), not an app-anatomy list. Pass. (6) FAQ "Who wrote the copy" — "The hero, the layer cards, the 'who it's for' section, the install CTA, and the timeline panel on the right are the human team's work" is a *page-anatomy* list (UI elements of this page), not an *app-anatomy* list. Pass. The combined test (functional-triad + shell-beat string scan) found no new violations across the 5 live files (2 sections + 3 posts + index). SEO minute re-run: titles all ≤60, descriptions all ≤155, og:* mirrored, site.js injected before </body> on every page, every internal link resolves. No pitch violations, no fabrication, rule 3 intact (shell untouched). No dream recall — rem/ still empty. No board state changes: NEXT is still 0, the add-run cadence bug at the runtime persists, the agent still has no legitimate ADD target until the team ships 05/06 or the env exposes $DATAFORSEO_AUTH. The audit is a no-fix passover this time, which is the *right* outcome after six prior audits caught and fixed the issues — the page is genuinely clean. No content/ or blog/ files changed; publish called for protocol consistency (the audit passover is part of the public record even when the content is unchanged). One audit commit, board log entry only.
- 2026-06-10 (waldo): audit — passover of the last 4 commits (the prior no-fix-clean-passover, the 1-fix FAQ shell-beat audit, the 3-fix FAQ+airtable audit, the board-groom plan). Re-audited all 5 live files with the combined functional-triad + shell-beat string scan; the page is genuinely clean — no new violations, every prior fix held, no drift. Specific checks: (1) 99-faq.html "What is a workbook" answer — the prior fix replaced the shell's person→team→world ladder and "the artifact is the same thing" beat with "The walk-through is the short version; the page's own blog is the long one." — still clean, no shell beat, no triad, no ladder. (2) 99-faq.html "Can I just use the web app?" — the prior fix removed "files" from "run the same workbook files" → "run the same workbooks" — still clean, the file-ness is gone. (3) airtable-alternative.html "branching" paragraph — all three items are same-category branching/conditional/event, passes the strict test. (4) retool-alternative.html "branching lives where the screen does" — all three items are same-category rules/branching/events, passes. (5) 04-the-workbook.html — paragraph 1's tech-stack list (HTML/manifest/CSS) and paragraph 3's readers-list (person/agent/auditor) are not app-anatomy lists; pass. (6) FAQ "Who wrote the copy" page-anatomy list is a UI-elements list, not an app-anatomy list; pass. SEO minute: titles (lovable 52, airtable 51, retool 47, blog 32 — all ≤60), descriptions (lovable ~145, airtable ~111, retool ~137, blog 140 — all ≤155), og:* mirrored, site.js injected before </body> on every page, every internal link resolves (topnote → /, deep-links → /#the-workbook, signoff → /blog/, GitHub links, the new /blog/ pointer in the FAQ is a known-good target). Design tokens used in every page (--ink/--dim/--faint/--live/--line/--panel/--mono) consistent with the shell's app.css :root. No pitch violations: no "sites that run themselves", no "software you can own", no fabrication. No dream recall — rem/ still empty. No board state changes: NEXT is still 0, the add-run cadence bug at the runtime persists, the agent still has no legitimate ADD target until the team ships 05/06 or the env exposes $DATAFORSEO_AUTH. The audit is a no-fix passover for the second time in a row, which is the *right* outcome — the page is genuinely clean. No content/ or blog/ files changed; publish called for protocol consistency (the audit passover is part of the public record even when the content is unchanged). One audit commit, board log entry only.
- 2026-06-11 (waldo): add — picked the top DOING task under long-game head terms. Fresh web_search on "desktop app builder" showed low-volume but real intent (no-code desktop, Tauri/Electron wrappers, FlutterFlow for desktop, Power Apps, Wappler). The SERP is dominated by no-code/low-code lists that stay in web/mobile shape or require engineering teams. Wedge: a desktop-first, open-source runtime that ships real programs (not prototypes) that a person and an agent edit together. Wrote the first long-game post targeting "desktop app builder" (low volume but uncontested). Added to blog/index.html (newest first), updated content/blog.json, and added a row to sections.org for future grown mirror if the band redesign opens a slot. Title 48 chars, description 148 chars, honest pre-launch, one-idea (the runtime is the builder), no banned beats. Self-review passed; wb content check clean. Resolves the desktop-app-builder task (promoted from DOING to DONE). Updated board to mark it.

* board
** DONE seed: ship 04-the-workbook
** DONE seed: ship 99-faq
** DONE seed: ship lovable blog + airtable blog + retool blog
** DONE seed: design the grown band as a set
** DONE long-game head terms — desktop app builder post shipped (first of the uncontested set)
*** TODO blog: internal app builder
*** TODO blog: self hosted retool
*** TODO blog: ai app coding
*** TODO blog: open source ai app builder (n/a volume)
** TODO next search-first content (researchable now via web_search)
*** TODO blog: build app with ai
*** TODO blog: v0 alternative
*** TODO blog: base44 vs lovable
** TODO proof-content + site completeness
*** TODO blog: agent maintains a website
*** TODO blog: anatomy of an audit run
*** TODO site: favicon (team scope)
*** TODO site: og:image on landing (team scope)
** CANCELLED seed: ship how-it-works section (dup of 01-the-stack)
** CANCELLED seed: on-the-fly AI content (pitch rule)
** CANCELLED seed: public API (product question for team)
