* board

** TODO Objective A — establish the page's spine (the three load-bearing beats)
   The page is a bare hero. Before flavor, it needs the three sections that
   carry the pitch as DESIGNED objects: what it is, how it works, the ask.
   Acceptance: a first-time reader, scrolling hero → A's sections, understands
   workbooks (apps) · runtime (runs them) · toolkits (authoring), sees the
   mechanism build itself, and can act — all gated PASS.

*** DONE system/layers section — the three layers as ONE drawn stack
    Shipped as 04-anatomy (runtime CMS partial). The Svelte grown lane
    (src/sections/grown/) is NOT wired into Home — nothing imports it, so a
    .svelte there would never reach the live page. The live lane is the CMS
    partial (content/sections.json → mountGrown). Built the stack STATIC: three
    overlapping .glass planes (z-index + graduated left-offset = real depth) +
    an SVG spine threading them, green only on the runtime layer (where the
    agent lives) + its breathing .alive dot + the live spine node. The static
    lane loses drift/draw/scrub but the depth + the breathe survive, and the
    relationship (a stack, not peers) reads. Idea owned: the architecture is a
    stack and the agent lives INSIDE the runtime, not beside it.

*** DONE mechanism section — "from a sentence to a living page"
    Shipped as 05-weave (static CMS partial). Three moves on a dashed flow
    line: you write intent → the agent weaves from toolkits → it runs and keeps
    running. Green arrives ONLY at the terminal node (life arrives at the end)
    + its .alive breathe. Same proven idiom as 04 but vertical/temporal, not a
    stack. Idea owned: the process by which a sentence becomes a living, running
    artifact — and you're reading one work.

*** TODO invitation section — build with your agent (the closing ask)
    The one primary CTA, framed as joining the living thing. Left-weighted,
    Fraunces line + one lede + the CTA (static partial → no magnetic; the CTA
    can still carry .alive breathe + sacred green). WMark beside. Idea: the ask.
    Must respect the pitch (rule 7) — malleability demo, never "sites that run
    themselves." pin AFTER the spine but before faq.

** TODO Objective B — give the organism proof and a voice
   Once the spine holds, prove the page is alive and let it speak honestly.
   Acceptance: the page contains at least one true living-proof and one
   honest field-note; nothing fabricated; both gated PASS.

*** TODO living-proof section — the agent actually running
    NOTE: the live timeline/cursor/panel are already the SHELL (team-owned, the
    right panel). A grown living-proof can't embed them. Likely this stays TODO
    or becomes a field-note pointing AT the live panel — do NOT fake it.
    Idea: see it living, right now.

*** TODO field-note — the organism's first journal entry
    Short, mono, dated, honest about being v1. col=narrow. This is where
    on-page blog-voice lives. Static partial survives here cleanly.
    Idea: the page has a diary, therefore it is worked-on / alive.

** TODO Objective C — compose, harden, and seed the supporting lane
   Make the whole page read as one inevitable organism; close the obvious
   doubts; open the SEO lane honestly.
   Acceptance: page has an faq pinned last, favicon + og resolve, and one
   search-first blog post exists with a working index.

*** TODO faq section — honest recurring doubts, pinned last
    Is this real? what edits the page? who reviews the agent? does it run
    locally? Plain Q&A, no green, pin:"last". Static partial OK.
    Idea: the page's composure — absorb the doubts so they stop leaking.

*** TODO favicon — inline SVG W (site 404s /favicon.ico)
    A small living W mark as favicon so the tab isn't broken. Match canon
    (warm-green W). Confirm it resolves. (May be shell-owned — check first.)

*** TODO og + meta — links unfurl well
    og:title / og:description / og:image (a banner) + canonical. (Likely
    shell-owned in index.html — check before assuming it's mine.)

*** TODO first blog post — "this website builds itself" (search-first)
    The supporting lane. Query-gate first (web_search the topic + volume),
    then a search-first, honest-about-v1 post in blog/. Create blog/index.html
    on first post + a small field-note/link to it on the page. NOT a
    thought-dump — it exists only if real searchers ask it.

*** TODO compose pass — whole-page rhythm once 4+ sections exist
    Re-read top→bottom, re-order sections, check the page reads as one growing
    organism with varied weight/pacing (not equal SaaS bands). Renumber
    partials, unify rhythm. Run the design gate as a second pass on each.

* log
- reset to bare hero — Living Machine redesign + Claude Opus; fresh start
- plan: built the roadmap from bare hero. 3 objectives, 11 tasks. Promoted the
  system/layers diagram to NEXT.
- add: 04-anatomy (system/layers). NEEDS: page (hero/mechanism/living-proof)
  had no architecture view; this owns the stack + "agent lives inside the
  runtime." ARCHETYPE: system/layers, static CMS partial (the Svelte grown lane
  is vestigial — nothing mounts src/sections/grown/; the working lane is
  content/sections.json). ONE IDEA: the system is a stack, not three peers, and
  the agent lives inside the runtime. COMPOSE: three overlapping .glass planes
  (z-index + left-offset = depth) + SVG spine; green only on runtime layer +
  breathing dot + live spine node; mono tags, .t-h2 head, Fraunces carries it;
  inline styles only (partials forbid <style>). GATE PASS A: depth 2 (stacked
  glass, real volume) · restraint 2 (one object, lots of air) · one-idea 2
  (fresh, no collision) · motion 1 (the .alive breathe is a true signature; the
  static lane loses drift/draw/unfold, heading inherits the shell's reveal) ·
  green 2 (sacred — runtime layer only) · earns 2 (load-bearing for the spine,
  type carries it) = 11/12, no bar at 0. PASS B triggers: prose-blob NO ·
  card-grid NO (overlapping stack + spine, not peer tiles) · slide-fade NO (the
  section's motion is the breathe; reveal is inherited chrome on the h2 only) ·
  green-decoration NO · centered NO (left-weighted, planes lean right) ·
  saas-rhythm NO (a depth object, not a band). 0 triggers. VERDICT PASS.
- add: 05-weave (mechanism). NEEDS: hero + 04-anatomy gave WHAT/structure but
  nothing owned the PROCESS — how a sentence becomes a living page. No
  collision (anatomy=static structure, weave=temporal flow). ARCHETYPE:
  mechanism, static CMS partial (live lane). ONE IDEA: a sentence becomes a
  living, running page and you watch it happen in three moves. COMPOSE: dashed
  SVG flow line threading three .glass moves (intent → weave → runs); green +
  .alive breathe ONLY on the terminal node (life arrives at the end); mono
  labels, .t-h2 Fraunces head, inline styles only. GATE PASS A: depth 2 (dashed
  line behind/through planes) · restraint 2 · one-idea 2 (fresh) · motion 1
  (.alive breathe true; static lane loses scrub/draw, head inherits reveal) ·
  green 2 (sacred — terminal living node only) · earns 2 = 11/12, no bar at 0.
  PASS B: prose-blob NO · card-grid NO (directional sequence) · slide-fade NO ·
  green-decoration NO · centered NO (left-weighted) · saas-rhythm NO. 0
  triggers. VERDICT PASS. wb content check clean (2 sections). publish ok (9
  files). Note: live fetch unverifiable — server at :4000 returns 404 at root
  (not running in sandbox); content check + publish are the gate this run.
