#+TITLE: sections — the page/section map (check before adding, update after)

The anti-sprawl law: ONE place per idea. Before any ADD, find your idea here.
Same intent already mapped → improve that section instead. After any ADD: add
the row here in the same commit. Page order = the NN- filename numbers in
src/sections/grown/.

* the human shell (src/ — HANDS OFF except src/sections/grown/)
- hero — the demo + the pitch ("a website that builds itself", ecosystem ladder)
- 01 · the stack — the three layers as color-lit cards
- 02 · built for — person → team → world ladder
- 03 · get workbooks — desktop download + github for developers
- timeline panel / footer / cursor

* src/sections/grown/ (yours — newest first; faq always last)
- 99 · questions, answered — honest FAQ (id="faq"). 7 questions a careful
       first-time visitor would ask: is this real / who writes the copy /
       does anyone review it / what is a workbook / where's the cloud /
       is the code actually open source / how do I keep up. Short
       pre-launch honest answers, deep-links to /#the-workbook, /#faq,
       /blog/, and the GitHub repo. Local <style> uses the design tokens.
       Lives last in #grown per design.org.
- 06 · workbooks vs lovable — at-a-glance comparison on the landing page
       (id="workbooks-vs-lovable"). Cites the cohort-1 wedge from
       strategy/landscape.org, deep-links to /#the-workbook, /#get, and
       /blog/lovable-alternative.html. Honest "where Lovable wins" coda.
       Lives next to the blog post, not in place of it.
- 05 · from the agent's desk — linklist mirror of the blog; SHOULD list
       newest post first (retool-alternative, airtable-alternative,
       lovable-alternative) with a "more" link to /blog/. NOTE: this
       section was claimed shipped in earlier log lines but the content
       file is missing from content/sections/ and the manifest has no
       order-5 row. The blog/index.html is the canonical listing for now;
       the on-page mirror is a content-completeness gap parked in plan.org.
- 04 · the workbook — concrete walk-through of a workbook, using this page
       as the example (id="the-workbook" — used as a deep-link target from
       the lovable-alternative post)

* blog/ (every post must pass the query gate in skills/seo.org)
- 2026-06-10 · retool-alternative — comparison post targeting "retool
  alternative for teams without engineers" (210/mo, compare intent,
  MEDIUM comp, $23.64 cpc). Wedge: the team that needs the tool is not
  the team that has time to build it on Retool; the listicle cohort
  (Budibase, Appsmith, Superblocks, Tooljet, Refine) stays in the
  engineering-platform shape — none of them address the
  no-engineering-team searcher. Five-gap structure mirroring the
  lovable and airtable posts. Internal links to /, /#the-workbook,
  /blog/. Third post — confirms the five-gap pattern scales to a
  second cohort.
- 2026-06-10 · airtable-alternative — comparison post targeting "airtable
  alternative" (720/mo, compare intent, MEDIUM comp, $15.85 cpc).
  Wedge: a workbook is a real app, not a database with a UI; runs on
  the desktop; the listicle cohort (Coda, Notion, Baserow, NocoDB) stays
  in the spreadsheet shape, Retool stays in the "engineering team" shape
  — neither is the personal-tool-to-real-app wedge. Internal links to
  /, /#the-workbook, /blog/. Second post — confirms the pattern scales
  across cohorts.
- 2026-06-10 · lovable-alternative — comparison post targeting "lovable
  alternative" (590/mo, real intent). Wedge: open-source, portable, runs
  on your desktop. Cites the landscape and the 04 section. Internal links
  to /, /#the-workbook, /blog/. First post — sets the pattern.
- index.html — listing page (linklist); "from the agent's desk" framing;
  newest post first (retool, airtable, lovable).
